---
const year = new Date().getFullYear();
---

<footer class="site-footer" role="contentinfo">
  <!-- ===== Callout / closing message ===== -->
  <section class="footer-callout" aria-labelledby="footer-cta-title">
    <h2 id="footer-cta-title" class="callout-title">Ready to Strengthen Your Claim?</h2>
    <p class="callout-copy">
      Our team stands with you. Get clear guidance, patient support, and a path forward—every step of the way.
    </p>
    <a href="/#get-started" class="callout-cta">Get Started Today</a>
  </section>

  <!-- ===== Disclaimer as its own subsection (above links) ===== -->
  <section class="footer-disclaimer" aria-labelledby="disclaimer-title">
    <div class="disclaimer-inner">
      <h2 id="disclaimer-title" class="disclaimer-title">Disclaimer</h2>
      <p class="disclaimer-text">
        Veterans Disability Network is a private organization and is not affiliated with, endorsed by, or sponsored by the
        U.S. Department of Veterans Affairs (VA), any state VA office, or any veterans’ service organization. By contacting us,
        you authorize us to reach you by phone, email, and SMS; message and data rates may apply. Our services are not legal or
        medical advice. Other organizations may provide similar assistance at no cost. Veterans may be eligible for additional
        benefits beyond those offered through our programs. Review our
        <a href="#" data-modal-trigger="privacy-policy">Privacy Policy</a> and <a href="#" data-modal-trigger="terms-conditions">Terms &amp; Conditions</a> for details.
      </p>
    </div>
  </section>

  <!-- ===== Main footer grid ===== -->
  <div class="footer-main">
    <!-- Brand / about -->
    <div class="col brand">
      <a href="/" class="brand-link" aria-label="Veterans Disability Network home">
        <img src="/logo_2.png" alt="" class="brand-logo" width="56" height="56" />
        <span class="brand-text">
          <strong class="brand-name">Veterans Disability Network</strong>
          <span class="brand-tag">Guiding Your Claim to Victory</span>
        </span>
      </a>
      <p class="brand-copy">
        Independent support for Veterans navigating the VA disability system. Clear answers. Compassionate guidance.
      </p>

      <ul class="contact" aria-label="Contact">
        <li>
          <svg class="icon" viewBox="0 0 24 24" aria-hidden="true"><path fill="currentColor" d="M6.6 10.2c1.2 2.3 3 4.1 5.3 5.3l1.8-1.8c.3-.3.8-.4 1.2-.2 1 .4 2 .6 3.1.6.7 0 1.2.5 1.2 1.2v2.9c0 .7-.5 1.2-1.2 1.2C10.4 20.4 3.6 13.6 3.6 5.2c0-.7.5-1.2 1.2-1.2H7.7c.7 0 1.2.5 1.2 1.2 0 1.1.2 2.1.6 3.1.1.4 0 .9-.3 1.2l-1.6 1.7Z"/></svg>
          <a href="tel:+18005551234">1‑800‑555‑1234</a>
        </li>
        <li>
          <svg class="icon" viewBox="0 0 24 24" aria-hidden="true"><path fill="currentColor" d="M2 4h20v16H2zM4 6l8 6 8-6"/></svg>
          <a href="mailto:vetteam@veteransdisabilitynetwork.net">vetteam@veteransdisabilitynetwork.net</a>
        </li>
        <li>
          <svg class="icon" viewBox="0 0 24 24" aria-hidden="true"><path fill="currentColor" d="M7 2h10a2 2 0 0 1 2 2v18l-7-3-7 3V4a2 2 0 0 1 2-2Z"/></svg>
          <span>Support Hours: 7 days / week</span>
        </li>
      </ul>

      <ul class="social" aria-label="Social">
        <li><a class="sbtn" href="#" aria-label="Follow on Facebook">
          <svg viewBox="0 0 24 24" class="sicon" aria-hidden="true"><path fill="currentColor" d="M13 3h4v4h-3c-.6 0-1 .4-1 1v3h4l-1 4h-3v7h-4v-7H6v-4h3V8c0-2.2 1.8-4 4-4Z"/></svg>
        </a></li>
        <li><a class="sbtn" href="#" aria-label="Follow on X">
          <svg viewBox="0 0 24 24" class="sicon" aria-hidden="true"><path fill="currentColor" d="M4 3h4.6l4 6.4L17 3h3.9l-6.9 9.7L22 21h-4.6l-4.5-7-5 7H3.1l7.2-10L4 3Z"/></svg>
        </a></li>
        <li><a class="sbtn" href="#" aria-label="Follow on LinkedIn">
          <svg viewBox="0 0 24 24" class="sicon" aria-hidden="true"><path fill="currentColor" d="M4 3a2 2 0 1 1 0 4 2 2 0 0 1 0-4Zm0 6h4v12H4V9Zm6 0h4v2h.1c.6-1.1 2.1-2.2 4.3-2.2 4.6 0 5.3 3 5.3 6.9V21h-4v-6.2c0-1.5 0-3.3-2-3.3s-2.3 1.6-2.3 3.2V21h-4V9Z"/></svg>
        </a></li>
      </ul>
    </div>

    <!-- Quick links -->
    <nav class="col links" aria-label="Quick links">
      <h3 class="col-title">Quick Links</h3>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/#about_us">About Us</a></li>
        <li><a href="/faq">FAQ</a></li>
        <li><a href="/#get-started">Get Started</a></li>
      </ul>
    </nav>

    <!-- Resources -->
    <nav class="col resources" aria-label="Resources">
      <h3 class="col-title">Resources</h3>
      <ul>
        <li><a href="#" data-modal-trigger="terms-conditions">Terms &amp; Conditions</a></li>
        <li><a href="#" data-modal-trigger="privacy-policy">Privacy Policy</a></li>
        <li><a href="#" data-modal-trigger="release-of-information">Release of Information</a></li>
      </ul>
    </nav>
  </div>

  <!-- ===== Legal bar ===== -->
  <div class="footer-legal">
    <p class="copyright">© {year} Veterans Disability Network. All rights reserved.</p>
    <a class="top" href="#site_top" aria-label="Back to top">Back to top ↑</a>
  </div>

  <style>
    /* Base */
    .site-footer {
      --footer-bg-1: #0a1430;
      --footer-bg-2: #071026;
      --footer-text: #eaf0ff;
      --footer-muted: #b9c4de;
      --footer-border: rgba(255,255,255,.12);

      background: linear-gradient(180deg, var(--footer-bg-1) 0%, var(--footer-bg-2) 100%);
      color: var(--footer-text);
      border-top: 1px solid var(--footer-border);
      margin-top: clamp(2rem, 6vw, 4rem);
    }

    /* Callout */
    .footer-callout {
      text-align: center;
      padding: clamp(1.75rem, 5vw, 3rem) 1rem 1rem;
      border-bottom: 1px solid var(--footer-border);
    }
    .callout-title {
      margin: 0 0 .5rem;
      font: 800 clamp(1.35rem, 2.2vw + 1rem, 2rem) "Merriweather", Georgia, serif;
      color: var(--color-primary);
      text-wrap: balance;
    }
    .callout-copy {
      margin: 0 auto 1rem;
      max-width: 60ch;
      color: var(--footer-muted);
      font-size: 1.05rem;
      line-height: 1.6;
    }
    .callout-cta {
      display: inline-block;
      padding: .85rem 1.5rem;
      border-radius: 9999px;
      font-weight: 700;
      text-decoration: none;
      background: var(--color-primary);
      color: var(--color-bg);
      box-shadow: 0 10px 24px rgba(0,0,0,.35);
      transition: transform .25s cubic-bezier(.34,1.56,.64,1), box-shadow .25s ease;
    }
    .callout-cta:hover, .callout-cta:focus-visible {
      transform: translateY(-2px) scale(1.03);
      box-shadow: 0 16px 32px rgba(0,0,0,.4);
      outline: none;
    }

    /* Disclaimer block */
    .footer-disclaimer {
      border-bottom: 1px solid var(--footer-border);
      padding: 1rem 0;
      background: linear-gradient(180deg, rgba(255,255,255,.02), rgba(255,255,255,.01));
    }
    .disclaimer-inner {
      max-width: 72rem;
      margin: 0 auto;
      padding: 0 1rem clamp(1rem, 2.5vw, 1.25rem);
    }
    .disclaimer-title {
      margin: .25rem 0 .5rem;
      font: 800 clamp(1.05rem, .6vw + 1rem, 1.2rem) "Merriweather", Georgia, serif;
      color: var(--footer-text);
      letter-spacing: .02em;
    }
    .disclaimer-text {
      margin: 0;
      color: var(--footer-muted);
      line-height: 1.65;
      font-size: 1rem;
      max-width: 80ch;
    }
    .footer-disclaimer a { color: var(--footer-text); text-decoration: underline; text-underline-offset: .15em; }

    /* Main grid */
    .footer-main {
      display: grid;
      gap: clamp(1.25rem, 2.5vw, 2rem);
      grid-template-columns: 1fr;
      padding: clamp(1.25rem, 4vw, 2rem) 1rem;
      max-width: 72rem;
      margin: 0 auto;
    }
    @media (min-width: 720px) {
      .footer-main { grid-template-columns: 1.2fr .8fr .8fr; }
    }

    .col-title {
      font: 800 1rem "Merriweather", Georgia, serif;
      margin: 0 0 .5rem;
      color: var(--footer-text);
      letter-spacing: .02em;
    }

    /* Brand column */
    .brand-link {
      display: inline-flex;
      align-items: center;
      gap: .65rem;
      text-decoration: none;
      color: inherit;
      margin-bottom: .35rem;
    }
    .brand-logo { width: 48px; height: 48px; object-fit: contain; }
    .brand-text { display: grid; }
    .brand-name { font-weight: 800; }
    .brand-tag { color: var(--footer-muted); font-size: .95rem; }
    .brand-copy { color: var(--footer-muted); margin: .5rem 0 1rem; line-height: 1.6; }

    .contact { list-style: none; margin: 0; padding: 0; display: grid; gap: .4rem; }
    .contact li { display: grid; grid-template-columns: 1.25rem auto; align-items: center; gap: .5rem; }
    .contact .icon { width: 18px; height: 18px; color: var(--color-primary); }
    .contact a { color: var(--footer-text); text-decoration: none; }
    .contact a:hover, .links a:hover, .resources a:hover { text-decoration: underline; text-underline-offset: .15em; }

    /* Lists */
    .links ul, .resources ul { list-style: none; margin: 0; padding: 0; display: grid; gap: .35rem; }
    .links a, .resources a { color: var(--footer-muted); text-decoration: none; }
    .links a:focus-visible, .resources a:focus-visible { outline: 2px solid var(--color-primary); outline-offset: 2px; }

    /* Social */
    .social { list-style: none; padding: 0; margin: .75rem 0 0; display: flex; gap: .5rem; }
    .sbtn {
      width: 38px; height: 38px; display: grid; place-items: center;
      border: 1px solid var(--footer-border); border-radius: .65rem;
      color: var(--footer-text); background: rgba(255,255,255,.04);
      text-decoration: none; transition: transform .2s ease, background .2s ease, border-color .2s ease;
    }
    .sbtn:hover, .sbtn:focus-visible { background: rgba(255,255,255,.1); border-color: rgba(255,255,255,.3); }
    .sicon { width: 18px; height: 18px; }

    /* Legal bar */
    .footer-legal {
      border-top: 1px solid var(--footer-border);
      display: flex; flex-wrap: wrap; gap: .5rem 1rem;
      align-items: center; justify-content: space-between;
      padding: 1rem;
      max-width: 72rem; margin: 0 auto;
      color: var(--footer-muted);
    }
    .top { color: var(--footer-text); text-decoration: none; }
    .top:hover, .top:focus-visible { text-decoration: underline; text-underline-offset: .15em; }

    @media (prefers-reduced-motion: reduce) {
      .callout-cta, .sbtn { transition: none; }
    }
  </style>
</footer>
