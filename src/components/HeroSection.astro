---
/* No props */
---

<div class="hero">
  <div class="hero-inner">
    <h1 class="hero-heading">
      Are You Tired of Not Getting the
      <span class="hero-highlight">VA&nbsp;Benefits</span> You Deserve?
    </h1>

    <p class="hero-tagline">
      We help Veterans navigate the claims process, fight unjust denials, and get the maximum disability compensation they deserve.
    </p>

    <a href="/get-started" class="hero-cta">Get Started Today</a>
  </div>

  <style>
    /* ───────────────── Hero (mobile‑first) ───────────────── */
    .hero {
      /* Use small viewport units so mobile browser UI doesn't squash the hero */
      min-height: calc(100svh - var(--header-h, 500px));
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;

      /* Comfortable mobile spacing + notch awareness */
      padding: clamp(3rem, 8vh, 5rem) 1rem
               max(2rem, calc(2rem + env(safe-area-inset-bottom)));
    }

    .hero-inner {
      /* Keep line length readable on small screens */
      max-width: 42rem;
      display: grid;
      gap: 1rem;
      animation: fade-slide-in .7s ease-out both;
    }

    /* ───────────────── Typography ───────────────── */
    .hero-heading {
      /* Never too small on phones; scales up smoothly */
      font-size: clamp(1.875rem, 6.5vw + 0.5rem, 3rem); /* ~30–48px */
      line-height: 1.15;
      font-weight: 800;
      font-family: "Merriweather", Georgia, serif;
      color: var(--color-bg);
      text-shadow: 0 3px 6px rgba(0,0,0,.4);
      margin: 0 0 .5rem;
      text-wrap: balance; /* nicer multi-line wrapping where supported */
    }

    .hero-highlight {
      color: var(--color-primary);
      text-decoration-color: currentColor;
      text-decoration-thickness: 3px;
      text-underline-offset: 4px;
    }

    .hero-tagline {
      /* Clear, readable copy on phones; modest growth on larger viewports */
      font-size: clamp(1rem, 3.2vw + .6rem, 1.25rem); /* ~16–20px */
      font-weight: 600;
      letter-spacing: .015em;
      color: var(--color-bg);
      margin: 0 0 1.75rem;
    }

    /* ───────────────── CTA ───────────────── */
    .hero-cta {
      display: inline-block;
      width: min(22rem, 100%);    /* full‑width on small screens, capped */
      padding: 1rem 1.25rem;
      border-radius: 9999px;
      font-weight: 700;
      font-size: 1rem;
      background: var(--color-primary);
      color: var(--color-bg);
      text-decoration: none;
      box-shadow: 0 6px 15px rgba(0,0,0,.25);
      transition:
        transform .3s cubic-bezier(.34,1.56,.64,1),
        box-shadow .3s ease;
      margin-inline: auto;
    }
    .hero-cta:hover,
    .hero-cta:focus-visible {
      transform: translateY(-3px) scale(1.04);
      box-shadow: 0 10px 20px rgba(0,0,0,.25);
    }
    .hero-cta:active {
      transform: translateY(0) scale(.97);
      box-shadow: 0 4px 10px rgba(0,0,0,.2);
    }

    /* ───────────────── Breakpoints ───────────────── */
    @media (min-width: 480px) {
      .hero-cta { width: auto; padding: 1rem 2rem; }
      .hero-inner { gap: 1.25rem; }
    }

    @media (min-width: 768px) {
      .hero {
        padding: clamp(6rem, 12vh, 9rem) 2rem 5rem;
        min-height: calc(100vh - 500px);
      }
      .hero-inner { gap: 1.5rem; }
      .hero-tagline { margin-bottom: 2.25rem; }
    }

    /* ───────────────── Reduced motion ───────────────── */
    @media (prefers-reduced-motion: reduce) {
      .hero-inner { animation: none; }
      .hero-cta { transition: background .25s ease, color .25s ease; }
    }
  </style>
</div>
