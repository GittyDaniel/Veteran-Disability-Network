

<section id="home">
<header class="site-header" role="banner">
	<div class="header-inner">
		<a href="/" class="brand" aria-label="Veterans Disability Network home">
			<img src="/logo.png" alt="" class="brand-logo" />
			<div class="brand-text">
				<span class="brand-name">Veterans Disability Network</span>
				<span class="brand-slogan">Guiding Your Claim to Victory</span>
			</div>
		</a>
		<nav class="nav" aria-label="Primary" id="primary-navigation">
      <ul>
        <li><a href="/" aria-current="page">Home</a></li>
        <li><a href="/benefits">Benefits by State</a></li>
        <li><a href="/faq">FAQ</a></li>
      </ul>
    </nav>
		<div class="header-actions">
			<a href="tel:+18005551234" class="cta" aria-label="Call us now at 1-800-555-1234">
          <svg width="18" height="18" aria-hidden="true" viewBox="0 0 24 24" focusable="false">
            <path fill="currentColor" d="M6.6 10.2c1.2 2.3 3 4.1 5.3 5.3l1.8-1.8c.3-.3.8-.4 1.2-.2 1 .4 2 .6 3.1.6.7 0 1.2.5 1.2 1.2v2.9c0 .7-.5 1.2-1.2 1.2C10.4 20.4 3.6 13.6 3.6 5.2c0-.7.5-1.2 1.2-1.2H7.7c.7 0 1.2.5 1.2 1.2 0 1.1.2 2.1.6 3.1.1.4 0 .9-.3 1.2l-1.6 1.7Z"/>
          </svg>
          <span>Call Us Now</span>
        </a>
		</div>
	</div>
</header>
<div class="hero">
  <div class="hero-inner">
    <h1 class="hero-heading">
      Are You Tired of Not Getting the
      <span class="hero-highlight">VA Benefits</span> You Deserve?
    </h1>

    <p class="hero-tagline">
      Ensure accurate filing on your first try with our guidance.
    </p>

    <a href="/get-started" class="hero-cta">Get Started Today</a>
  </div>
</div>
</section>
<!-- FEATURE STRIP — place directly after the hero -->
<section class="stats-strip" role="region" aria-label="Key service metrics">
  <ul class="stats-list">
    <li class="stat-item">
      <span class="stat-value">21,000+</span>
      <span class="stat-label">Veterans&nbsp;Served</span>
    </li>
    <li class="stat-item">
      <span class="stat-value">20 – 30%</span>
      <span class="stat-label">Avg. Rating Increase</span>
    </li>
    <li class="stat-item">
      <span class="stat-value">100%</span>
      <span class="stat-label">Dedicated&nbsp;to&nbsp;Veterans</span>
    </li>
    <li class="stat-item">
      <span class="stat-value">24&nbsp;/&nbsp;7</span>
      <span class="stat-label">Availability</span>
    </li>
  </ul>
</section>


<style>
	 #home {
    color: #fff;

    /* Gradient first, image second */
    background:
      var(--gradient-hero-overlay),
      url('../assets/hero_bg_3.webp') center/cover no-repeat;
  }


	.site-header {
    background: var(--color-bg);
    border-bottom: 1px solid var(--color-border);
    position: relative;
    z-index: 10;
		width: 100%;
		box-shadow: 0 3px 5px rgba(115, 146, 193, 0.3);
  }

	.header-inner {
    max-width: 100%;
    margin: 0 auto;
    display: flex;
    gap: 1rem;
    align-items: center;
		justify-content: space-between;
    padding: 1rem 3rem;
  }

	.brand {
    display: flex;
    align-items: center;
    text-decoration: none;
    gap: .75rem;
    min-width: 0;
  }

  .brand-logo {
    width: 80px;
    height: 80px;
    display:block;
		object-fit: contain;
  }

  .brand-text {
    display: flex;
    flex-direction: column;
    line-height: 1.1;
  }
  .brand-name {
    font-family: "Merriweather", Georgia, serif;
    font-weight: 700;
    font-size: 1.6rem;
    color: var(--color-primary);
  }
  .brand-slogan {
    font-size: 1rem;
    font-style: italic;
    color: var(--color-muted);
  }

	.nav {
    display: flex;
    align-items: center;
    gap: 2rem;
  }

	.nav ul {
    list-style: none;
    display: flex;
    gap: 1.5rem;
    margin: 0;
    padding: 0;
  }
	/* === Animated underline for nav links === */
.nav a {
  position: relative;      /* establish containing block for ::after */
  font-weight: 600;
  color: var(--color-text);
  text-decoration: none;   /* kill default underline */
  padding: .5rem .25rem;
  border-radius: .25rem;
  overflow: hidden;        /* hide the line while collapsed */
  /* Optional: shorter tap target on mobile */
}

.nav a::after {
  content: "";
  position: absolute;
  bottom: 0;               /* sits at link’s baseline */
  left: 0;
  width: 100%;
  height: 2px;             /* thickness of the underline */
  background: var(--color-primary);
  transform: scaleX(0);    /* collapsed by default */
  transform-origin: left;  /* grows from the left edge */
  transition: transform .3s cubic-bezier(.4,0,.2,1); /* subtle ease */
}

/* Hover / focus / current page — expand the line */
.nav a:hover::after,
.nav a:focus-visible::after,
.nav a[aria-current="page"]::after {
  transform: scaleX(1);
}

/* Keep the text color change you already use */
.nav a:hover,
.nav a:focus-visible {
  color: var(--color-primary);
}

/* === Outlined call‑to‑action button === */
.header-actions {
  display: flex;
  align-items: center;
}

/* Base look */
.cta {
  --cta-border: 2px;
  display: inline-flex;
  align-items: center;
  gap: .5rem;
  font-weight: 600;
  padding: .6rem 1.25rem;
  border: var(--cta-border) solid var(--color-primary);
  color: var(--color-primary);
  background: transparent;
  border-radius: 9999px;          /* pill shape */
  text-decoration: none;
  transition:
    background-color .25s ease,
    color            .25s ease,
    transform        .25s cubic-bezier(.34,1.56,.64,1); /* gentle “pop” */
}

/* Hover / focus animation */
.cta:hover,
.cta:focus-visible {
  background: var(--color-primary);
  color: var(--color-bg);
  transform: translateY(-2px) scale(1.03);
  box-shadow: 0 6px 12px rgba(0,0,0,.12);
}

/* Active (press) state */
.cta:active {
  transform: translateY(0) scale(.98);
  box-shadow: 0 2px 4px rgba(0,0,0,.12);
}

/* === HERO SECTION === */
.hero {
  /* re‑use existing gradient / bg from #home */
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 12rem 2rem 4rem;
  min-height: calc(100vh - 500px); /* responsive */
}

.hero-inner {
  max-width: 55rem;
  animation: fade-slide-in .8s ease-out both; /* subtle entry */
}

.hero-heading {
  font-size: clamp(2rem, 4.5vw, 3.5rem);
  line-height: 1.15;
  font-weight: 800;
  font-family: "Merriweather", Georgia, serif;
  color: var(--color-bg);           /* light text on dark bg */
  text-shadow: 0 3px 6px rgba(0,0,0,.4);
  margin-bottom: 1rem;
}

.hero-highlight {
  color: var(--color-primary);
  text-decoration-color: currentColor;
  text-decoration-thickness: 3px;
  text-underline-offset: 4px;
}

.hero-tagline {
  font-size: clamp(1.15rem, 1.6vw + .7rem, 1.5rem); /* larger, fluid */
  font-weight: 400;
	font-style: normal;
  letter-spacing: .015em;
  color: var(--color-bg);          /* full body text color for better contrast */
  margin-bottom: 2.5rem;
}

/* CTA button – filled style that complements outlined header button */
.hero-cta {
  display: inline-block;
  padding: .9rem 2.5rem;
  border-radius: 9999px;
  font-weight: 700;
  font-size: 1rem;
  background: var(--color-primary);
  color: var(--color-bg);
  text-decoration: none;
  box-shadow: 0 6px 15px rgba(0,0,0,.25);
  transition:
    transform .3s cubic-bezier(.34,1.56,.64,1),
    box-shadow .3s ease;
}
.hero-cta:hover,
.hero-cta:focus-visible {
  transform: translateY(-3px) scale(1.04);
  box-shadow: 0 10px 20px rgba(0,0,0,.25);
}
.hero-cta:active {
  transform: translateY(0) scale(.97);
  box-shadow: 0 4px 10px rgba(0,0,0,.2);
}

/* Subtle fade / slide animation */
@keyframes fade-slide-in {
  from {opacity:0; transform: translateY(30px);}
  to   {opacity:1; transform: translateY(0);}
}

/* === FEATURE STRIP === */
.stats-strip {
  background: var(--color-bg);
  border-top: 1px solid var(--color-border);
  border-bottom: 1px solid var(--color-border);
  padding: 2.5rem 1rem;
}

.stats-list {
  --gap: 3rem;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: var(--gap) var(--gap);
  max-width: 70rem;
  margin: 0 auto;
  list-style: none;
  padding: 0;
}

.stat-item {
  flex: 1 1 12rem;            /* 4‑up on desktop, 2‑up on tablets */
  text-align: center;
  animation: fade-slide-in .8s ease-out both; /* re‑uses earlier keyframes */
}

.stat-value {
  display: block;
  font-size: clamp(1.75rem, 3vw + 1rem, 2.5rem);
  font-weight: 800;
  color: var(--color-primary);
  line-height: 1.1;
}

.stat-label {
  display: block;
  font-size: 1rem;
  font-weight: 600;
  color: var(--color-text);
  letter-spacing: .02em;
  margin-top: .25rem;
}

/* Reduced motion */
@media (prefers-reduced-motion: reduce) {
  .stat-item { animation: none; }

  .hero-inner { animation: none; }
  .hero-cta { transition: background .25s ease, color .25s ease; }
  .cta { transition: background-color .2s linear, color .2s linear; }
  .cta:hover,
  .cta:focus-visible,
  .cta:active { transform: none; }
  .nav a::after { transition: none; }
}
</style>
